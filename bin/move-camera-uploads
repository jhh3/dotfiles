#!/bin/bash
# Move camera uploads to external harddrive if possible

if mount | grep JHH-TMEXT > /dev/null; then
	echo "Moving camera uploads"
	rsync --remove-source-files -av /Users/jhh3/Dropbox/Camera\ Uploads/ /Volumes/JHH-TMEXT/Camera\ Uploads/
else
	echo "Drive not connected."
fi

